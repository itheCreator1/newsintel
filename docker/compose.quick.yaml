services:
  backend-test:
    network_mode: none
    environment:
      NEWSINTEL_RUN_POSTGRES_TESTS: "0"
      NEWSINTEL_ELASTICSEARCH_URL: http://127.0.0.1:1

  frontend-test:
    network_mode: none
